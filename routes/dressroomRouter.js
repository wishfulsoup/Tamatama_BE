const express = require('express');

const dressroomController = require('../controllers/dressroomController');

const router = express.Router();

// router.get('', dressroomController.getItems);
// router.get('/detail/:id', dressroomController.getItemDetails);

module.exports = router;
